[project]
name = "seller-pulse"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Biswajit" }
]
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.8.0",
    "chromadb>=1.5.9",
    "gradio>=6.28.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
seller-pulse = "seller_pulse:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
